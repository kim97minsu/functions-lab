let calculate = (x, y, operation) => {
	return (x, y, operation)
}
if(operation = "add") {
	return (x + y)
} if(operation = "subtract") {
	return (x - y)
} if(operation = "multiply") {
	return (x * y)
} if(operation)
